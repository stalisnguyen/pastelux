{
  "name": "pastelux",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "An architectural lighting design knowledge base with a daily industry digest.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "news": "node scripts/fetch-news.mjs",
    "check": "node scripts/check-links.mjs",
    "check:strict": "node scripts/check-links.mjs --strict",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.6",
    "@astrojs/sitemap": "^3.3.1",
    "@tailwindcss/vite": "^4.1.7",
    "astro": "^5.8.0",
    "tailwindcss": "^4.1.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.3.0",
    "typescript": "^5.9.3"
  },
  "overridesComment": "@tailwindcss/vite resolves Vite 8 while Astro 5 bundles Vite 6, and the two Plugin types are structurally incompatible. Pinning both to Astro's version clears the type error; the build works either way.",
  "overrides": {
    "vite": "^6.4.3"
  }
}
